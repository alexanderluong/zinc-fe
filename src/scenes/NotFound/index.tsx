import * as React from "react";
import NotFound from "./components";

export const NotFoundScene: React.FC<{}> = () => {
  return (
    <React.Fragment>
      <NotFound />
    </React.Fragment>
  );
};
